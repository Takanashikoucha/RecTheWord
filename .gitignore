# Python
__pycache__/
*.py[cod]
*.pyc
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Model caches (downloaded at install/runtime from ModelScope) — never commit.
# The lightweight SenseVoice-Small is fetched by install.ps1 (step 7) so the
# repo stays small enough for plain git (GitHub 100MB/file limit).
models/

# App data (never commit)
recordings/
sessions/
transcripts/
logs/
user_settings.json
config.json
*.wav
*.mp3

# Legacy (superseded by LiveTranslate base)
_legacy_*/

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# test / local venv
.pytest_cache/
.venvtest/

# agent state
.dsh-state.md
