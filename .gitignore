# Python
__pycache__/
*.py[cod]
*.pyc
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.uv-cache/

# Model caches (downloaded at install/runtime from ModelScope) — never commit.
# Models are fetched by install.ps1 (ModelManager) so the repo stays small.
# The single documented exception (qfuxa streaming tower) is also runtime-fetched.
models/
.modelscope_cache/
.hf_home/

# App data (never commit)
recordings/
sessions/
transcripts/
logs/
user_settings.json
config.json
*.wav
*.mp3

# Local staging / scratch (never commit)
_dl/
tmp/
*:memory:*

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Tests are local-only (not shipped in the repo)
tests/

# Agent state
.dsh-state.md
