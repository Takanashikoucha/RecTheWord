# RecTheWord 重构状态文件

## 计划
- 基座落地（clone LiveTranslate）：in_progress
- 双路采集改造：pending
- 透明窗双栏两级显示 + 文字流：pending
- 会话管理：pending
- 录音三文件落盘：pending
- 精修 + 标注 UI + 纪要：pending
- 翻译双向化：pending
- 测试 + 文档：pending

## 发现
- 用户决策：fork LiveTranslate 改造（其 Windows 链路已验证）；FFmpeg 路线弃用；深度重构；两路分开显示；三语（中日英）whisper 自动检测。
- LiveTranslate 架构：PyAudioWPatch(WASAPI 回环+mic mix-in) → Silero VAD(32ms) → ASR worker 池(faster-whisper/FunASR/SenseVoice/远程) → OpenAI 兼容翻译(流式) → PyQt6 透明 overlay。
- 其 audio_capture.py 原生支持 mic 通道（_mic_stream/_mic_buf），改造点是不混音、拆双队列。
- 远程 ASR 协议：POST /transcribe body=[uint32 lang_len][lang][float32 PCM 16k mono]；GET /health。
- 差异化新增：_recorder/_transcript_log/_sessions/_session_ui/_refine_view/_minutes/_offline_diarize/_labels。
- 停止后零后台计算；精修/纪要手动触发；会话目录 ~/.rectheword/sessions/<ts>/{meta,mix/mic/sys.wav,transcript.jsonl,refined.json,labels.json,minutes.md}。
- 两级显示：interim 灰即时、final 定色原位替换（seq 匹配）；翻译只跟 final。

## 进展
- 轮1：调研完毕（本地旧代码 + LiveTranslate README/REMOTE_ASR/audio_capture/main/requirements/LICENSE 结构）。
- 轮2：计划 v4 获批准。

## 操作指纹
- curl raw.githubusercontent.com LiveTranslate README：1 次，成功
- curl REMOTE_ASR.md：1 次，成功
- curl audio_capture.py + main.py：1 次，成功
- curl vad_processor.py：1 次，返回空（head 截断或分支名问题，未深究，改用 README 描述）
- duckduckgo 搜索 whisper 日语：1 次，成功

## 轮次预算
- 重构总任务：预算 40，已用 2

## 最近 3 轮进展
- 轮1：有（调研完成，关键架构事实落盘）
- 轮2：有（计划批准）
- 轮3：有（开始执行）

## 后台任务
（无）

## 沙盒权限
（全部工作区内，无需审批）

## 阻塞点
（无）
