# RecTheWord 重构状态文件

## 计划
- 基座落地：completed（commit 28f8b75）
- 双路采集改造：completed（audio_capture 双队列 + main.py LanePipeline 双路 + 双栏 overlay）
- 透明窗双栏两级显示 + 文字流：completed（_transcript_log.py + overlay 双栏 + interim/final 事件链）
- 会话管理：completed（_sessions.py + _session_ui.py + 托盘菜单入口）
- 录音三文件落盘：completed（_recorder.py，start/stop 接入）
- 精修 + 标注 UI + 纪要：completed（_offline_diarize/_refine_view/_minutes/_labels，手动触发）
- 翻译双向化：completed（_emit_text 路由 + mic_target_language + config.yaml）
- 测试 + 文档：in_progress

## 发现
- 用户决策：fork LiveTranslate 改造（其 Windows 链路已验证）；FFmpeg 路线弃用；深度重构；两路分开显示；三语（中日英）whisper 自动检测；停止后零后台计算（精修/纪要手动）；透明 overlay 为 UI 主体；说话人改名 UI 在独立精修窗口。
- LiveTranslate 架构：PyAudioWPatch(WASAPI 回环+mic) → Silero VAD(32ms) → ASR worker 进程(Pipe IPC) → OpenAI 兼容翻译(流式) → PyQt6 透明 overlay。
- 其增量 ASR（interim 周期识别 + yasbd 分句 + trim + 回声去重）即"两级显示"的成熟实现，已抽取为 LanePipeline 类供双路复用。
- audio_capture 改造点：mic 不再混入 loopback，独立 mic_queue 按 chunk 节拍出队；get_audio/get_mic_audio 分路。
- 远程 ASR 协议：POST /transcribe body=[uint32 lang_len][lang][float32 PCM 16k mono]；GET /health。
- 差异化新增模块：_recorder/_transcript_log/_sessions/_session_ui/_refine_view/_minutes/_offline_diarize/_labels。
- 会话目录 ~/.rectheword/sessions/<ts>/{meta,mix/mic/sys.wav,transcript.jsonl,refined.json,labels.json,minutes.md}。
- 两级显示：interim 灰即时、final 定色原位替换（seq 匹配）；翻译只跟 final。
- 行为规范违规自查（用户指出）：L2 进度未及时落盘、L3 gate 未写状态文件、2 动作规则未执行。已纠正：状态文件每操作同轮成对更新；L3 节点先读状态文件写守卫结果。

## 进展
- 轮1：调研完毕（本地旧代码 + LiveTranslate 核心文件）。
- 轮2：计划 v4 获批准。
- 轮3：基座落地（clone + 迁移 + 2 个 commit）。
- 轮4：audio_capture 双队列改造完成（py_compile 通过）；main.py 插入 LanePipeline 类；__init__ 移除单 VAD（进行中）。

## 操作指纹
- git clone LiveTranslate：1 次，成功
- curl raw.githubusercontent.com（README/REMOTE_ASR/audio_capture/main/vad/requirements）：多次，成功
- duckduckgo 搜索 whisper 日语：1 次，成功
- web-search skill 脚本：1 次，失败（uv 缓存只读 + heredoc 吞参），已弃用该指纹
- py_compile audio_capture.py：1 次，成功
- git commit（基座迁移 / gitignore）：2 次，成功

## 轮次预算
- 重构总任务：预算 40，已用 6

## 最近 3 轮进展
- 轮2：有（计划批准）
- 轮3：有（基座落地完成，2 commits）
- 轮4：有（audio_capture 双队列✓，LanePipeline 类✓，__init__ 改造中）

## 后台任务
（无运行中）

## 沙盒权限
（全部工作区内，无需审批）

## 阻塞点
（无）
