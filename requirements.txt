# RecTheWord v2 — 由 install.ps1 用 uv 安装（uv pip install -r requirements.txt）
qwen-asr
onnxruntime
numpy
soundfile
scipy
httpx
pyyaml
PySide6
modelscope
pyaudiowpatch; platform_system == "Windows"   # WASAPI 采集/环回（设备枚举 + 热切换）
